# Path to the Firebase service account JSON (downloaded from Firebase Console)
GOOGLE_APPLICATION_CREDENTIALS=./firebase-service-account.json

# Firebase Realtime Database URL (same as the one in your existing .ino sketch)
DATABASE_URL=https://home-automation-a86aa-default-rtdb.firebaseio.com

# Email that should have admin privileges
ADMIN_EMAIL=chitrang313@gmail.com

# Port the backend listens on
PORT=5000

# CORS origin (frontend dev URL)
CORS_ORIGIN=http://localhost:5173
